name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  ci:
    uses: runcycles/.github/.github/workflows/ci-java.yml@main
    with:
      pom-file: cycles-protocol-service/pom.xml
